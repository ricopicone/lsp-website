# Byte-compiled / optimized files
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/

# Django
db.sqlite3
db.sqlite3-journal
/staticfiles/
/media/
*.log

# Environment files and secrets
.env
*.pem

# Test and coverage artifacts
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# Editor and OS files
.DS_Store
.idea/
.vscode/

# One-shot import staging (member rosters, headshot mappings). Lives only on
# the workstation; ship to prod via scp/SSM.
/import-staging/

# Source data drops that carry member info (e.g. the analyst-availability
# sheet). Kept on the workstation for import; never committed.
/*Availability*.pdf

# Tailwind/DaisyUI CSS build pipeline
node_modules/
/static/css/site.css
/private-media/

# Exported member-suggestion briefs (handed to Claude Code sessions locally)
/suggestions-briefs/
