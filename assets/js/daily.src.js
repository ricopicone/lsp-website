// Vendored Daily.co client — bundled by esbuild to static/js/vendor/daily.js
// (committed; no runtime CDN, mirroring the TipTap doc editor). Rebuild with
// `npm run build:js` after bumping @daily-co/daily-js. Exposes the Prebuilt
// factory as window.DailyIframe for video/templates/video/room.html.
import DailyIframe from "@daily-co/daily-js";

window.DailyIframe = DailyIframe;
